PORT=4000

# Optional. If unset, AI normalization is skipped and the app runs fully on OSM baseline data.
ANTHROPIC_API_KEY=

# Cache TTLs (seconds)
GEOCODE_CACHE_TTL=604800
OSM_CACHE_TTL=43200
SEARCH_RESULT_CACHE_TTL=900
